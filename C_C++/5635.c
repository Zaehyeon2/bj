#include <stdio.h>
#include <string>

int main(int argc, char const *argv[]) {
  int T;
  int Ym,
  for (size_t i = 0; i < T; i++) {
    int D, M, Y;
    char name[31];
    scanf("%s %d %d %d", &name, &D, &M, &Y);
  }
  return 0;
}
