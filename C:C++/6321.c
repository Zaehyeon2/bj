#include <stdio.h>

int main(int argc, char const *argv[]) {
  int N;
  char Name;
  scanf("%d", &N);
  for (size_t i = 0; i < N; i++) {
    scanf("%s", &Name);
    for (size_t j = 0; j < count; j++) {
      /* code */
    }
  }
  return 0;
}
